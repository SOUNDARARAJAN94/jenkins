pipeline {
    agent any 

    stages {

        stage('Clone') {
            steps {
                echo 'Code Cloned Successfully'
            }
        }

        stage('Build') {
            steps {
                echo 'Build Successful'
            }
        }

        stage('Deploy') {
            steps {
                echo 'Deployment Successful'
            }
        }
    }
}
